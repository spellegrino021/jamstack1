name: Trigger auto deployment for stpelleg-cont2

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ takyyon-patch-1 ]
    paths:
    - '//**'
    - '.github/workflows/stpelleg-cont2-AutoDeployTrigger-212c4e9f-ce09-4159-b185-7d50cd195dc9.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.STPELLEGCONT2_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.STPELLEGCONT2_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.STPELLEGCONT2_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: containerregistryvoj5arwja3y5y.azurecr.io
          registryUsername: ${{ secrets.STPELLEGCONT2_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.STPELLEGCONT2_REGISTRY_PASSWORD }}
          containerAppName: stpelleg-cont2
          resourceGroup: jamstack1ba97
          imageToBuild: containerregistryvoj5arwja3y5y.azurecr.io/stpelleg-cont2:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


